interface:
  display_name: "Systemic Fix"
  short_description: "Investigate issues and plan durable fixes"
  default_prompt: "Investigate this issue from evidence, classify it as local refactor, architectural remediation, or tracked debt, compare credible options, and return the most durable fix plan."
